import React, { FunctionComponent } from 'react';

import ContactsList from 'features/social/containers/contacts-list';

const Root: FunctionComponent = () => {

  return (
    <ContactsList />
  );
};

export default Root;
